# #296 step 25: the touch-target script is named verbatim in docs/STYLE.md:154,
# beside the pytest suite that enforces the same idiom. AGENTS.md keeps the test
# path — the one an agent runs — and drops the script.
duplicate :: scripts/check-touch-targets.sh

# --- 2026-09-23 curation (#309 follow-up): host memory split to docs/HOST-MEMORY.md.
# All three are verbatim in docs/HOST-MEMORY.md — the resolve command in its
# SocratiCode pin block, the two directives as `MemoryLow=512M` / `OOMScoreAdjust=-500`.
AGENTS.md :: duplicate :: mcp-driver.mjs resolve
AGENTS.md :: duplicate :: MemoryLow=
AGENTS.md :: duplicate :: OOMScoreAdjust=
# The DEPLOYMENT.md pointer into the moved section now links HOST-MEMORY.md.
docs/DEPLOYMENT.md :: retarget :: #host-memory-posture-307
