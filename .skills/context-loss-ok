# Judged losses from the #276 AGENTS.md curation.
# Each line below was read against its replacement before being warranted.
# Warrant `duplicate`: the dropped detail is verbatim in the destination doc the
# surviving pointer names — confirmed by grep, not by assumption.

# --- Infrastructure / the bus -> docs/ARCHITECTURE.md "Redis and the bus" ---
# All four published streams are enumerated there (content.fetch,
# content.fetch-policy, content.revisions, info.watch-status).
duplicate :: **The bus.** Archiver operates the broker. Watcher publishes `content.fetch`

# --- Server Lifecycle -> docs/COMMANDS.md "Development" (L83-86) ---
# Carries the #233 rationale, the _test/_dev refusal and src/core/db_safety.py.
# The constraint itself stays inline; only the rationale moved out.
duplicate :: **Never launch uvicorn by hand with the prod env loaded** — it would share the prod DB

# --- Watched Items / empty extraction -> docs/CONTENT-PIPELINE.md ---
# The headline rule stays inline; the #253 provenance columns are in the doc.
duplicate :: **Empty extraction is a failure, not a change (#258).** When every `source_spec`
duplicate :: yields empty chunks, `process_watched_item` raises `ExtractionError` and writes
duplicate :: nothing — unconditionally, on both sides of a baseline. Rationale, and the six
duplicate :: provenance columns the outbox gained for `source_revision_observed` (#253):

# --- Watched Items / conditional GET -> docs/CONDITIONAL-GET.md, CONTENT-PIPELINE.md ---
# Gate, 304-inherits-fingerprint and invalidation stay inline in compressed form;
# invalid_request_options and the #275 re-issue cap are in the linked docs.
duplicate :: **Conditional GET is gated and item-scoped (#269).** Watcher replays each item's
duplicate :: stored `etag`/`last_modified`, but only for items named in
duplicate :: `WATCHER_CONDITIONAL_GET_ENABLED` (unset → off, byte-identical to the pre-#269
duplicate :: command). A 304 inherits the last fingerprint, so a spec/URL/extractor change
duplicate :: must invalidate the pair: [docs/CONDITIONAL-GET.md](docs/CONDITIONAL-GET.md).
duplicate :: Never route `invalid_request_options` past the validator clear — the refusal
duplicate :: precedes the request, so a bad stored value wedges the item permanently. An unreadable
duplicate :: blob's re-issue is capped for a related reason — it bypasses the scheduling
duplicate :: gate (#275): [docs/CONTENT-PIPELINE.md](docs/CONTENT-PIPELINE.md).

# --- Conventions / logging -> docs/CONVENTIONS.md (L11-12, L16-18) ---
# The four-key floor and its test stay inline; the --log-config and
# strip_color_message mechanics are in the doc.
duplicate :: Records are JSON with a four-key floor — `timestamp`/`level`/`logger`/`message` — pinned by `tests/core/test_logging.py`; don't rename or drop a key

# --- Watched Items / registry -> docs/WATCHED-ITEMS.md ---
# §Registry linkage and lifecycle carries the POST's four requirements, the #260
# source_specs floor and the "not gated on source_specs" residual; §Domain keying
# carries the domain_name == Domain.name == hostname(effective_url) equality and
# the one-entry-per-hostname rule; §Registry reconciliation (#254) enumerates the
# authoritative columns MORE precisely than this line did (it adds "and only when
# the host actually moves"). The one fact unique to AGENTS.md — no caller since
# archiver#158 (2026-08-17) — was MOVED into §Registry linkage and lifecycle in
# this same change, not dropped.
duplicate :: **The `info.registry` reconcile is the creation path** — it creates from an announcement alone
duplicate :: **The registry owns cadence and active state; Watcher owns mechanism (#254).** An announcement is authoritative for exactly five columns

# --- #278 CR round 2: Environment Files -> docs/ENVIRONMENT.md ---------------
# The notifier-credential paragraph was tightened to the rule alone; every
# dropped detail was grepped in the destination before warranting, per the
# header of this file. docs/ENVIRONMENT.md carries "600 root:root", the
# load-env.sh exemption, "unreadable as `exedev`", the ~1289 delivered
# notifications, "Slack and Mailgun", and both WATCHER_DEV_NOTIFIER_* names.
# The surviving inline text keeps the rule and points at that doc.
duplicate :: A third file, `/etc/watcher/notifier.env` (600 root:root), holds
duplicate :: `WATCHER_NOTIFIER_BASE_URL` + `WATCHER_NOTIFIER_API_KEY` and is loaded by
duplicate :: `deploy/watcher.service` alone — never by `load-env.sh`, and unreadable as
duplicate :: `exedev`. **Do not source it, copy it, or re-add those two variables to a shared
duplicate :: env file** (#278): the gate below stops an inherited credential being *used*, but
duplicate :: ~1289 watcher fixture notifications had already been delivered on it to
duplicate :: production Slack and Mailgun, and a credential nothing holds cannot be spent.
duplicate :: Non-production runs use notifier's development tenant via
duplicate :: `WATCHER_DEV_NOTIFIER_BASE_URL` / `WATCHER_DEV_NOTIFIER_API_KEY` in `.env`.

# The two pointers below named docs/DEPLOYMENT.md → *Environment Variables*,
# which this same run moved to docs/ENVIRONMENT.md. The corrected lines are in
# AGENTS.md verbatim apart from the new target.
retarget :: **Naming rule for new variables.** Anything naming a shared external resource takes a **service-prefixed** name with a separate dev key (`WATCHER_BUS_REDIS_URL`
retarget :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — systemd units, environment variables, wheelhouse auth

# --- #274 curation: the announcement-audit paragraph returns to docs/ ---
# Three lines are the audit prose itself. Every claim in them is carried, more
# fully, by docs/WATCHED-ITEMS.md → "What an announcement records (#274)":
# the audit row per applied announcement, `watched_item.created` with
# `source: "registry"`, `announcement_applied` with its `changes` diff, the
# non-empty condition, and the silent `0-0` boot replay. Read against the
# destination before warranting, not inferred from the topic.
AGENTS.md :: duplicate :: Every applied announcement is audited: a create as
AGENTS.md :: duplicate :: (`source: registry`), an update as `watched_item.announcement_applied`
AGENTS.md :: duplicate :: `changes` diff — emitted **only when non-empty**
# The remaining three are reflow, not loss: removing the sentence above re-wraps
# the paragraph around it. Every surviving word — the `POST` caller note, the
# archiver#158 reference, the whole pointer list — is still inline in AGENTS.md,
# on differently-bounded lines. The pointer's dropped topic ("the
# spec-acknowledgement meaning of `last_reviewed_at`") is covered by the same
# destination section.
AGENTS.md :: duplicate :: boot replay stay silent (#274). `POST /api/v1/watched-items` still works
AGENTS.md :: duplicate :: but has had no caller since archiver#158. What each 409 is, the authoritative
AGENTS.md :: duplicate :: column list, the spec-acknowledgement meaning of `last_reviewed_at`, schedule

# --- #280 curation: the #191 collapse residue -> docs/WATCHED-ITEMS.md -------
# The enumeration of what the collapse removed was demoted verbatim into
# "WatchEvent identity fields" (L326-329), which already carried the #191
# rename residue — grepped there, not assumed. The rule itself stays inline:
# "**The `WatchedItem` is the single monitored entity (#191).**" is untouched at
# AGENTS.md L114, and "one URL = one fingerprint = one change signal" survives
# on the re-wrapped line beneath it. The two boundary lines are re-wrap, not
# loss — every surviving word is still inline, on differently-bounded lines,
# which the closed warrant set spells `duplicate` (same reading the #274
# entries above take).
AGENTS.md :: duplicate :: table, its `/watches*` routes, the override resolution chain and the per-Watch
AGENTS.md :: duplicate :: one URL = one fingerprint = one change signal; the earlier `Watch` model — its
AGENTS.md :: duplicate :: notification tier — is gone. The user-facing noun is "Watched

# --- #280 curation: two pointers to docs/WATCHED-ITEMS.md merged into one ----
# The section named the same doc twice and listed "schedule resolution" in both.
# The merged pointer at AGENTS.md L141 carries every topic word from both, none
# dropped: fields, what each 409 is, the authoritative column list, schedule
# resolution, domain keying, media-type dispatch, template CRUD. Only the
# duplicate link and the repeated topic are gone.
AGENTS.md :: duplicate :: archiver#158. What each 409 is, the authoritative column list, schedule
AGENTS.md :: duplicate :: resolution, domain keying: [docs/WATCHED-ITEMS.md](docs/WATCHED-ITEMS.md).
AGENTS.md :: duplicate :: Fields, schedule resolution, media-type dispatch, template CRUD: [docs/WATCHED-ITEMS.md](docs/WATCHED-ITEMS.md). Lifecycle, delete guards, every dashboard surface: [docs/WATCHED-ITEMS-DASHBOARD.md](docs/WATCHED-ITEMS-DASHBOARD.md).

# --- #280 curation: the Detail Docs parenthetical -> docs/ARCHITECTURE.md ----
# Added earlier on this same branch, then removed here: docs/ARCHITECTURE.md
# "Sibling services" now opens with the notifier relocation in full, so the
# index entry was restating its own destination. The index line is otherwise
# verbatim what it was before this branch.
AGENTS.md :: duplicate :: - [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — module layout, sibling services (notifier is off-VM, over the tailnet), the Archiver checkout constraint, bus topology and fetch contracts

# --- 2026-09-04 curation (#287 CR follow-up): AGENTS.md 6170 -> under budget ---
# Every line below was read against its replacement or its destination before
# being warranted. `duplicate` follows this file's established meaning: the
# dropped detail is in the destination doc the surviving index entry names,
# confirmed by grep, not by assumption.

# Class C, Infrastructure. The unit/port fact was stated three times — here, in
# the port table immediately below it, and again as the first line of Server
# Lifecycle. The clause was dropped; both other statements stand.
tighten :: Systemd service `watcher` runs the live site on port 8000.

# Class C, Watched Items. Narrative history of how the convention arose: the
# `Watch` model no longer exists, so nothing can be reached for by that name.
# What it was and what its collapse removed is docs/WATCHED-ITEMS.md
# "What the #191 collapse removed" (:326).
tighten :: one URL = one fingerprint = one change signal; the earlier `Watch` model is
tighten :: gone. The user-facing noun is "Watched

# Class B/D, Watched Items -> docs/CONDITIONAL-GET.md, which the Detail Docs
# index already names as "gate, snapshot, invalidation". Conditional GET is off
# by default and item-scoped, so it is needed on some tasks rather than nearly
# every one. Evidence is quoted, not located: that doc has no `##` headings, and
# a line number rots on the next edit to it, sending a re-judger somewhere
# unrelated and letting them conclude this warrant was wrong (CR round 2,
# finding 12). Grep the destination for:
#   gate            "The gate (`WATCHER_CONDITIONAL_GET_ENABLED`) is off for this item"
#   the 304 trap    "A 304 produces no bytes, so"
#   stale extractor "inheriting a fingerprint the *old* extractor computed"
duplicate :: **Conditional GET is gated and item-scoped (#269)** — off unless the item is
duplicate :: named in `WATCHER_CONDITIONAL_GET_ENABLED`, and a 304 inherits the last
duplicate :: fingerprint, so a spec/URL/extractor change must invalidate the stored pair:
duplicate :: [docs/CONDITIONAL-GET.md](docs/CONDITIONAL-GET.md).

# Class B/D, Watched Items -> docs/WATCHED-ITEMS.md, which the Detail Docs index
# already names as covering notifications. Visibility semantics at
# "## Notification visibility" (:266); the block-structured Markdown rule and
# the same TestMarkdownListContract guard at "## Notification body format"
# (:302), in more detail than the inline copy carried.
duplicate :: **Notifications.** One `notification_templates` table; a row's `visibility` —
duplicate :: `global` / `domain` / `watched_item` — decides where it fires. **Bodies are
duplicate :: source Markdown and must be block-structured**, never `\n`-joined — guarded by
duplicate :: `tests/core/notifications/test_content.py::TestMarkdownListContract`.
