# Judged losses from the #276 AGENTS.md curation.
# Each line below was read against its replacement before being warranted.
# Warrant `duplicate`: the dropped detail is verbatim in the destination doc the
# surviving pointer names — confirmed by grep, not by assumption.

# --- Infrastructure / the bus -> docs/ARCHITECTURE.md "Redis and the bus" ---
# All four published streams are enumerated there (content.fetch,
# content.fetch-policy, content.revisions, info.watch-status).
duplicate :: **The bus.** Archiver operates the broker. Watcher publishes `content.fetch`

# --- Server Lifecycle -> docs/COMMANDS.md "Development" (L83-86) ---
# Carries the #233 rationale, the _test/_dev refusal and src/core/db_safety.py.
# The constraint itself stays inline; only the rationale moved out.
duplicate :: **Never launch uvicorn by hand with the prod env loaded** — it would share the prod DB

# --- Watched Items / empty extraction -> docs/CONTENT-PIPELINE.md ---
# The headline rule stays inline; the #253 provenance columns are in the doc.
duplicate :: **Empty extraction is a failure, not a change (#258).** When every `source_spec`
duplicate :: yields empty chunks, `process_watched_item` raises `ExtractionError` and writes
duplicate :: nothing — unconditionally, on both sides of a baseline. Rationale, and the six
duplicate :: provenance columns the outbox gained for `source_revision_observed` (#253):

# --- Watched Items / conditional GET -> docs/CONDITIONAL-GET.md, CONTENT-PIPELINE.md ---
# Gate, 304-inherits-fingerprint and invalidation stay inline in compressed form;
# invalid_request_options and the #275 re-issue cap are in the linked docs.
duplicate :: **Conditional GET is gated and item-scoped (#269).** Watcher replays each item's
duplicate :: stored `etag`/`last_modified`, but only for items named in
duplicate :: `WATCHER_CONDITIONAL_GET_ENABLED` (unset → off, byte-identical to the pre-#269
duplicate :: command). A 304 inherits the last fingerprint, so a spec/URL/extractor change
duplicate :: must invalidate the pair: [docs/CONDITIONAL-GET.md](docs/CONDITIONAL-GET.md).
duplicate :: Never route `invalid_request_options` past the validator clear — the refusal
duplicate :: precedes the request, so a bad stored value wedges the item permanently. An unreadable
duplicate :: blob's re-issue is capped for a related reason — it bypasses the scheduling
duplicate :: gate (#275): [docs/CONTENT-PIPELINE.md](docs/CONTENT-PIPELINE.md).

# --- Conventions / logging -> docs/CONVENTIONS.md (L11-12, L16-18) ---
# The four-key floor and its test stay inline; the --log-config and
# strip_color_message mechanics are in the doc.
duplicate :: Records are JSON with a four-key floor — `timestamp`/`level`/`logger`/`message` — pinned by `tests/core/test_logging.py`; don't rename or drop a key

# --- Watched Items / registry -> docs/WATCHED-ITEMS.md ---
# §Registry linkage and lifecycle carries the POST's four requirements, the #260
# source_specs floor and the "not gated on source_specs" residual; §Domain keying
# carries the domain_name == Domain.name == hostname(effective_url) equality and
# the one-entry-per-hostname rule; §Registry reconciliation (#254) enumerates the
# authoritative columns MORE precisely than this line did (it adds "and only when
# the host actually moves"). The one fact unique to AGENTS.md — no caller since
# archiver#158 (2026-08-17) — was MOVED into §Registry linkage and lifecycle in
# this same change, not dropped.
duplicate :: **The `info.registry` reconcile is the creation path** — it creates from an announcement alone
duplicate :: **The registry owns cadence and active state; Watcher owns mechanism (#254).** An announcement is authoritative for exactly five columns

# --- #278 CR round 2: Environment Files -> docs/ENVIRONMENT.md ---------------
# The notifier-credential paragraph was tightened to the rule alone; every
# dropped detail was grepped in the destination before warranting, per the
# header of this file. docs/ENVIRONMENT.md carries "600 root:root", the
# load-env.sh exemption, "unreadable as `exedev`", the ~1289 delivered
# notifications, "Slack and Mailgun", and both WATCHER_DEV_NOTIFIER_* names.
# The surviving inline text keeps the rule and points at that doc.
duplicate :: A third file, `/etc/watcher/notifier.env` (600 root:root), holds
duplicate :: `WATCHER_NOTIFIER_BASE_URL` + `WATCHER_NOTIFIER_API_KEY` and is loaded by
duplicate :: `deploy/watcher.service` alone — never by `load-env.sh`, and unreadable as
duplicate :: `exedev`. **Do not source it, copy it, or re-add those two variables to a shared
duplicate :: env file** (#278): the gate below stops an inherited credential being *used*, but
duplicate :: ~1289 watcher fixture notifications had already been delivered on it to
duplicate :: production Slack and Mailgun, and a credential nothing holds cannot be spent.
duplicate :: Non-production runs use notifier's development tenant via
duplicate :: `WATCHER_DEV_NOTIFIER_BASE_URL` / `WATCHER_DEV_NOTIFIER_API_KEY` in `.env`.

# The two pointers below named docs/DEPLOYMENT.md → *Environment Variables*,
# which this same run moved to docs/ENVIRONMENT.md. The corrected lines are in
# AGENTS.md verbatim apart from the new target.
retarget :: **Naming rule for new variables.** Anything naming a shared external resource takes a **service-prefixed** name with a separate dev key (`WATCHER_BUS_REDIS_URL`
retarget :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — systemd units, environment variables, wheelhouse auth

# --- #274 curation: the announcement-audit paragraph returns to docs/ ---
# Three lines are the audit prose itself. Every claim in them is carried, more
# fully, by docs/WATCHED-ITEMS.md → "What an announcement records (#274)":
# the audit row per applied announcement, `watched_item.created` with
# `source: "registry"`, `announcement_applied` with its `changes` diff, the
# non-empty condition, and the silent `0-0` boot replay. Read against the
# destination before warranting, not inferred from the topic.
# The remaining three are reflow, not loss: removing the sentence above re-wraps
# the paragraph around it. Every surviving word — the `POST` caller note, the
# archiver#158 reference, the whole pointer list — is still inline in AGENTS.md,
# on differently-bounded lines. The pointer's dropped topic ("the
# spec-acknowledgement meaning of `last_reviewed_at`") is covered by the same
# destination section.

# --- #280 curation: the #191 collapse residue -> docs/WATCHED-ITEMS.md -------
# The enumeration of what the collapse removed was demoted verbatim into
# "WatchEvent identity fields" (L326-329), which already carried the #191
# rename residue — grepped there, not assumed. The rule itself stays inline:
# "**The `WatchedItem` is the single monitored entity (#191).**" is untouched at
# AGENTS.md L114, and "one URL = one fingerprint = one change signal" survives
# on the re-wrapped line beneath it. The two boundary lines are re-wrap, not
# loss — every surviving word is still inline, on differently-bounded lines,
# which the closed warrant set spells `duplicate` (same reading the #274
# entries above take).

# --- #280 curation: two pointers to docs/WATCHED-ITEMS.md merged into one ----
# The section named the same doc twice and listed "schedule resolution" in both.
# The merged pointer at AGENTS.md L141 carries every topic word from both, none
# dropped: fields, what each 409 is, the authoritative column list, schedule
# resolution, domain keying, media-type dispatch, template CRUD. Only the
# duplicate link and the repeated topic are gone.

# --- #280 curation: the Detail Docs parenthetical -> docs/ARCHITECTURE.md ----
# Added earlier on this same branch, then removed here: docs/ARCHITECTURE.md
# "Sibling services" now opens with the notifier relocation in full, so the
# index entry was restating its own destination. The index line is otherwise
# verbatim what it was before this branch.

# --- 2026-09-04 curation (#287 CR follow-up): AGENTS.md 6170 -> under budget ---
# Every line below was read against its replacement or its destination before
# being warranted. `duplicate` follows this file's established meaning: the
# dropped detail is in the destination doc the surviving index entry names,
# confirmed by grep, not by assumption.

# Class C, Infrastructure. The unit/port fact was stated three times — here, in
# the port table immediately below it, and again as the first line of Server
# Lifecycle. The clause was dropped; both other statements stand.
tighten :: Systemd service `watcher` runs the live site on port 8000.

# Class C, Watched Items. Narrative history of how the convention arose: the
# `Watch` model no longer exists, so nothing can be reached for by that name.
# What it was and what its collapse removed is docs/WATCHED-ITEMS.md
# "What the #191 collapse removed" (:326).
tighten :: one URL = one fingerprint = one change signal; the earlier `Watch` model is
tighten :: gone. The user-facing noun is "Watched

# Class B/D, Watched Items -> docs/CONDITIONAL-GET.md, which the Detail Docs
# index already names as "gate, snapshot, invalidation". Conditional GET is off
# by default and item-scoped, so it is needed on some tasks rather than nearly
# every one. Evidence is quoted, not located: that doc has no `##` headings, and
# a line number rots on the next edit to it, sending a re-judger somewhere
# unrelated and letting them conclude this warrant was wrong (CR round 2,
# finding 12). Grep the destination for:
#   gate            "The gate (`WATCHER_CONDITIONAL_GET_ENABLED`) is off for this item"
#   the 304 trap    "A 304 produces no bytes, so"
#   stale extractor "inheriting a fingerprint the *old* extractor computed"
duplicate :: **Conditional GET is gated and item-scoped (#269)** — off unless the item is
duplicate :: named in `WATCHER_CONDITIONAL_GET_ENABLED`, and a 304 inherits the last
duplicate :: fingerprint, so a spec/URL/extractor change must invalidate the stored pair:
duplicate :: [docs/CONDITIONAL-GET.md](docs/CONDITIONAL-GET.md).

# Class B/D, Watched Items -> docs/WATCHED-ITEMS.md, which the Detail Docs index
# already names as covering notifications. Visibility semantics at
# "## Notification visibility" (:266); the block-structured Markdown rule and
# the same TestMarkdownListContract guard at "## Notification body format"
# (:302), in more detail than the inline copy carried.
duplicate :: **Notifications.** One `notification_templates` table; a row's `visibility` —
duplicate :: `global` / `domain` / `watched_item` — decides where it fires. **Bodies are
duplicate :: source Markdown and must be block-structured**, never `\n`-joined — guarded by
duplicate :: `tests/core/notifications/test_content.py::TestMarkdownListContract`.

# --- Judged losses from the #292 curation round -------------------------------
# Three class-C tightenings, all verified with `--claims`: 141 atoms at base, 0
# dropped. Read that correctly (CR 8): `claims_dropped: 0` means no atom is
# "present at base and nowhere now" — the SURFACE, not this file. Seven of them
# did leave AGENTS.md (`deps.py`, `flash_oob`, `min-h-[44px]`, `min-h-0`,
# `agentskills.io`, `.claude/skills/`, `44px`) and now live only in the
# destination docs the surviving pointers name. A later re-judge should grep the
# destination, not AGENTS.md, before calling one of these entries stale.
#
# The editorial rule applied: prose is load-bearing only where no guard is.
# `no title attributes` has no automated guard (checked: nothing in tests/ or
# scripts/ forbids the attribute), so it stays inline stated as the rule with
# nothing behind it. The touch-target idiom and HTMX detection each have guards
# that fail the build, so their depth moved to the pointer.

# --- Style & UI / Accessibility -> docs/STYLE.md "7. Touch Targets" ---
# Dropped: the `min-h-[44px]` / `.btn` / `min-h-0` enumeration. docs/STYLE.md §7
# carries it in more detail (11 `44px` hits, 4 `min-h-[44px]`, 2 `min-h-0`),
# including the #203 reference and the full list of classes that bake it in.
# The two guard paths survive inline, which is what an agent needs to act.
tighten :: **Accessibility:** WCAG 2.1 AA, and no `title` attributes. **Touch-target idiom (#203):**

# --- Style & UI / HTMX -> docs/UI.md "3. Flash / Notification UX" ---
# Dropped: the `src/dashboard/deps.py` link and the `partials/flash_oob.html`
# path. docs/UI.md carries both (1 `is_htmx`, 1 `HX-Request`, 3 `flash_oob`).
# `is_htmx(request)` itself is KEPT inline — it is the actionable atom, and the
# point of the line is that an agent writes that call rather than reads a header.
tighten :: **HTMX:** **Detect HTMX with `is_htmx(request)`** ([src/dashboard/deps.py](src/dashboard/deps.py))

# --- Agent Skills -> docs/SKILLS.md (Directory Layout, Local Overrides, Linked Projects) ---
# Two base lines merged into one. Dropped: the `skills/` + `.claude/skills/`
# layout enumeration and the agentskills.io attribution — docs/SKILLS.md carries
# all of it (11 `skills-vendor` hits, 2 agentskills.io, 1 settings.local.json,
# 1 "Linked Projects"). The shadowing rule and the settings.local.json
# requirement stay inline: those are the two facts that change what an agent does.
tighten :: Skills live in `skills/` (agentskills.io) and `.claude/skills/` (Claude Code).
tighten :: Cross-project search to the sister `notifier` index requires a per-instance

# ============================================================================
# #296 step 25 curation (AGENTS.md 6252 -> 5975). Every entry below is a class-C
# rewrite in place: the claims survive, the words do not. --claims passes, so
# each line's atoms are accounted for somewhere in the surface.
# ============================================================================

# --- Environment & Tooling: the Tailwind pin, said in fewer words ---

# --- Code Exploration Policy: two lines merged into one, same two rules ---
# The health-hook clause is KEPT verbatim inside the merged line: docs/SKILLS.md
# names it deliberate belt-and-braces, so it is not a candidate for removal.
tighten :: SocratiCode is indexed on this repo
tighten :: A second, daily health hook **reports only**
tighten :: For broad semantic questions

# --- Infrastructure / Server Lifecycle: same rules, less connective tissue ---
tighten :: **Connection policy (#287, #288, #290).**
tighten :: Do not directly edit sibling repos

# --- Common Commands: one word ---
tighten :: fails rather than defaulting
tighten :: to production.

# --- Watched Items #254: where pause lives and the callerless POST both move
# to docs/WATCHED-ITEMS.md, which already carries them (lines 144-150, 246).
tighten :: item-level pause lives in Archiver's
tighten :: dashboard alone, and every announcement-owned field
tighten :: still works but has had no caller since
tighten :: archiver#158. What each 409 is:

# --- Watched Items #293: the mechanism (horizon freeze, upsert target) lives in
# docs/CONTENT-PIPELINE.md § "A renewed blob reference is re-announced (#293)".
# The two invariants an agent must not get wrong — never the baseline, and a
# renewal may only improve a row — stay inline.
tighten :: A full fetch renews the
tighten :: blob behind the latest revision
tighten :: otherwise Archiver's stored horizon freezes
tighten :: a stable item becomes unreplicable
tighten :: observation of the pair, not a refresh
tighten :: improve a queued row:

# --- Style & UI: five prose bullets -> five imperative rules. Each rule's detail
# was already authoritative in docs/STYLE.md / docs/UI.md, and in fuller form
# there than here (UI.md:361 carries the HX-Boosted guard this never mentioned).
tighten :: Component library and the
tighten :: Both authoritative; what follows
tighten :: is only what is easy to get wrong.
tighten :: **Brand:** Cannabis Observer
tighten :: **Dark Mode:** Tailwind `dark:` variants
tighten :: **Accessibility:** WCAG 2.1 AA, and no
tighten :: **CSS:** Tailwind v4 with `@theme`
tighten :: **HTMX:** **detect with `is_htmx(request)`**

# --- #296 step 25, second pass: after the fresh-eyes review restored five rules
# that the first pass had weakened, these paid for them. All wayfinding or
# connective tissue; no rule changed scope.
tighten :: **Single process is load-bearing.**
tighten :: **The bus.** Archiver operates the broker
tighten :: **A URL is configuration, not permission.** Three unit-only opt-ins gate the production
tighten :: the Archiver checkout constraint, bus topology and fetch contracts
tighten :: #288 the `noeviction` cap and the
tighten :: fetch → extract → fingerprint, the fetch-command outbox
tighten :: logging configuration, ULID error handling, DB-trigger rules
tighten :: the manual upgrade step, the two-role grant model
tighten :: the component library and the HTMX/flash interaction patterns
tighten :: the entity: fields, schedule resolution, registry reconciliation
tighten :: routes, lifecycle guards, list and detail views, audit parity

# --- 2026-09-23 curation (6474 -> under 6000). Every line below was rewritten in
# place, not dropped: each one's atoms are carried by the new line or by the doc
# it points at (--claims passes). Read against its replacement one by one.
tighten :: Client contract and traps: [docs/SOCRATICODE.md](docs/SOCRATICODE.md); goal→tool table
tighten :: **Host memory is the shared resource (#307).** Measured 2026-09-18
tighten :: **Retention is sized against the set (#292).** A config/state cap is floored per batch
tighten :: **The `info.registry` reconcile is the creation path**, and the registry owns
tighten :: cadence and active state while Watcher owns mechanism (#254): an announcement is
tighten :: authoritative for a named set of columns, everything else survives reconciliation,
tighten :: and **a local pause is not sticky** — every announcement-owned field 409s
tighten :: locally on a reconciled item. What each 409 is, where pause does live, and the
tighten :: `POST /api/v1/watched-items` — still the only creation path, with no caller
tighten :: since archiver#158:
tighten :: Fields, what each 409 is, the authoritative column list, schedule resolution, domain keying
AGENTS.md :: tighten :: [docs/WATCHED-ITEMS.md](docs/WATCHED-ITEMS.md).
tighten :: **Empty extraction is a failure, not a change (#258).** Every `source_spec`
tighten :: yielding empty chunks raises `ExtractionError` and writes nothing —
tighten :: unconditionally, on both sides of a baseline:
AGENTS.md :: tighten :: **[docs/CONTENT-PIPELINE.md](docs/CONTENT-PIPELINE.md)**.
tighten :: **An unchanged fingerprint still announces (#293)** — after a **full fetch**,
tighten :: which renews the blob, so its cache-hit branch upserts a `PendingArchiverSync`
tighten :: for the latest revision. A 304 renews nothing and announces nothing. **Never
tighten :: Records are JSON with a four-key floor — `timestamp`/`level`/`logger`/`message`
tighten :: template** — both carry every rule below in fuller form than this file did:
tighten :: brand color is never a status color (STYLE §2), every color utility takes its
tighten :: `dark:` variant (§3), WCAG 2.1 AA and no `title` attributes (§7–8), never a CDN
tighten :: build (§10), component classes over raw utilities (UI §4), and
tighten :: `is_htmx(request)` rather than a bare `HX-Request` read (UI §2, #211).
tighten :: - [docs/SKILLS.md](docs/SKILLS.md) — skill triggers, vendored skill repos, SocratiCode workflow
tighten :: - [docs/WATCHED-ITEMS.md](docs/WATCHED-ITEMS.md) — the entity: fields, schedule resolution, reconciliation, notifications
# ruff's F set (pyproject select) flags a star import as F403.
default :: - Explicit imports only
# Class D: restates a trained default; no rule of this repo's rides on it.
default :: - Small, focused functions

# --- 2026-09-23: RECOVERY-REHEARSALS.md indexed by extending RECOVERY.md's line.
AGENTS.md :: index :: - [docs/RECOVERY.md](docs/RECOVERY.md) — nightly DB backup to GCS, restore, go/no-go gates

# --- 2026-09-23 curation (#309 follow-up): host memory split to docs/HOST-MEMORY.md.
# Class C, --claims-checked: host-memory line retargeted to the new doc and its
# verify/re-pin tail and GiB figure dropped (in HOST-MEMORY.md); the bus line drops
# the content.blobs group derivation (ARCHITECTURE.md carries `group_name`, #285);
# retention folds its repeated ARCHITECTURE.md pointer into "same section".
AGENTS.md :: tighten :: **Host memory is the shared resource (#307).** 7.75 GiB, no swap, and agent sessions are *unkillable* (`oom_score_adj` -1000), so the kernel's killer takes the service. SocratiCode is **pinned pre-installed** (`~/.socraticode/pin`): never let a launch install a server. The unit takes a **reservation, never a cap** (`MemoryLow=`/`OOMScoreAdjust=`; `MemoryHigh=` stalls it while it still reports `active`), holding only while **every slice above grants as much** (#309). Verify with `preflight.sh --check` and `mcp-driver.mjs resolve`; re-pin as a decision, not on a schedule: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) → *Host memory posture*.
AGENTS.md :: tighten :: **The bus.** The broker is its own VM (`broker`, CannObserv/broker); watcher publishes four streams and consumes two — `content.blobs` (single-member group `watcher.blobs`, derived by co-core's `group_name` — #285) and `info.registry` (**groupless**, replayed from `0-0` every boot). `WATCHER_BUS_REDIS_URL` unset → publish tasks skip loudly. Inventory, ownership, fetch contracts, `info_source_id` on the wire: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) → *Redis and the bus*.
AGENTS.md :: tighten :: **Retention is sized against the set (#292), and mirrored on the broker (#319).** `tests/test_bus_stream_kinds.py` fails a config/state publish missing `maxlen` *or* `floor=`, and fails a retune of any mirrored number — the broker's probe thresholds are computed from copies, so the fix is cross-repo. The env overrides move the same numbers unseen: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) → *Redis and the bus*.
# The split itself: the moved heading loses its #307 slug (a class 3b seam),
# and the two pointers into it follow the section to HOST-MEMORY.md.
docs/DEPLOYMENT.md :: rename :: ### Host memory posture (#307)
docs/DEPLOYMENT.md :: retarget :: > [§ Host memory posture](#host-memory-posture-307).
docs/DEPLOYMENT.md :: retarget :: # docs/DEPLOYMENT.md -> Host memory posture.

# --- #325 prep: CONTENT-PIPELINE.md split -> docs/CONTENT-REVISIONS.md ---
# The index line's "the outbox, the revisions producer" now describes the new
# doc, which carries its own index line; CONTENT-PIPELINE keeps the fetch half.
AGENTS.md :: retarget :: - [docs/CONTENT-PIPELINE.md](docs/CONTENT-PIPELINE.md) — fetch → extract → fingerprint, the outbox, the revisions producer
