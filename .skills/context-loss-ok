# Lines this curation had to rewrite rather than move, each judged against its
# replacement. Format: WARRANT :: CONTENT (see prove-no-loss.sh --help).

# The bus paragraph moved to docs/ARCHITECTURE.md in this same change, so the
# pointer sentence that named it was rewritten to lead with the new summary.
# The words survive in AGENTS.md ("Stream ownership, the fetch contracts, and
# `info_source_id` on the wire: docs/ARCHITECTURE.md"); only the line's leading
# "silently." — a wrap artefact of the paragraph that moved — is gone.
retarget :: Stream ownership, the fetch contracts, and `info_source_id` on the wire:

# CR round 2, finding 10: the "At a glance" bus summary was dropped from
# docs/ARCHITECTURE.md as restatement. These lines are the AGENTS.md bus
# paragraph they had been moved into. Each claim below was checked individually
# against "## Redis and the bus", which states all of them at greater length:
# the four publish streams, info.watch-status as levels-not-edges with the
# republish cron and the never-an-ack-path rule, content.blobs as a
# single-member group, info.registry groupless and replayed from 0-0 via
# AsyncBusTailReader (never $), Archiver operating the broker, and the loud
# skip when WATCHER_BUS_REDIS_URL is unset. AGENTS.md also still carries a
# compressed inline summary, so no reader loses the overview.
# NOTE: this is judged content-equivalence, not a byte match — challenge it by
# reading "## Redis and the bus" against these lines.
duplicate :: **The bus.** Watcher publishes `content.fetch` (commands), `content.fetch-policy`
duplicate :: (per-host politeness), `content.revisions` (`source_revision_observed`), and
duplicate :: `info.watch-status` (#264 — the registry channel's return leg: applied generation,
duplicate :: scheduler state, observation freshness; levels-not-edges, full-set republish on
duplicate :: `WATCHER_WATCH_STATUS_REPUBLISH_CRON`, never an ack path); consumes
duplicate :: `content.blobs` as the single member of consumer group `watcher`; and consumes
duplicate :: `info.registry` **grouplessly** — a config/state stream replayed from `0-0` at every boot
duplicate :: via `AsyncBusTailReader`, never `$` (a worker that boots at `$` reads nothing and looks
duplicate :: exactly like one whose registry is empty). Archiver operates the
duplicate :: broker. `WATCHER_BUS_REDIS_URL` unset → the publish tasks skip loudly rather than

# Directive 13: docs/WATCHED-ITEMS.md split. The index entry named "lifecycle
# guards, dashboard surfaces" among that doc's contents; this same change moved
# both into docs/WATCHED-ITEMS-DASHBOARD.md, so the line was rewritten into two
# entries — one per doc — and both now describe what they actually point at.
retarget :: the WatchedItem entity: fields, schedule resolution, registry reconciliation, lifecycle guards, dashboard sur

# Directive 13 follow-on: the AGENTS.md "Watched Items" detail was compressed
# once docs/WATCHED-ITEMS.md had headroom from the split. Every claim in these
# lines was checked individually against that doc before warranting, and the
# two it did NOT cover were written into it first (§ Registry linkage and
# lifecycle): creation from an announcement alone / cold-start convergence, and
# the POST no longer validating the InfoItem over HTTP (archiver#141). Checking
# also turned up a stale claim there — "One create path" — which #254 had
# refuted; corrected to two in the same edit.
# As with the bus block above, this is judged content-equivalence rather than a
# byte match: read "## Registry reconciliation" and "## Registry linkage and
# lifecycle" against these lines to challenge it.
duplicate :: Created two ways since #254: `POST /api/v1/watched-items` (still Archiver's provisioning
duplicate :: call, requiring all three of `archiver_info_item_id` + `url` + `archiver_info_source_id`;
duplicate :: no dashboard create form) and the `info.registry` reconcile, which creates from an
duplicate :: announcement alone so a cold start converges from the snapshot. The POST no longer
duplicate :: validates the InfoItem over HTTP — that was the last outbound call and it went with the
duplicate :: SDK — so it is redundant once archiver#141's producer is live.
duplicate :: `WatchedItem.domain_name` == `Domain.name` == `hostname(effective_url)` by construction;
duplicate :: one entry per hostname, so host variants (`lcb.wa.gov` vs `www.lcb.wa.gov`) are
duplicate :: independent by design.
duplicate :: **The registry owns cadence and active state; Watcher owns mechanism (#254).** An
duplicate :: announcement is authoritative for exactly five columns — `archiver_info_source_id`,
duplicate :: `domain_name` and its denormalized state, and only when the host actually moves.
duplicate :: Everything else survives reconciliation: health, timings, `domain_suspended`,
duplicate :: `archived_at`, `throttle_floor_interval`, `default_schedule_config`, media type, tags,
duplicate :: notification config. **A local pause is not sticky** — item-level pause lives in
duplicate :: Archiver's dashboard alone; local backoff, `domain_suspended`, and the throttle floor are
duplicate :: the legitimate local stops. On a **reconciled** item (`applied_generation` set) every
duplicate :: announcement-owned field 409s locally — `is_active`, `effective_url`, `source_specs`,
duplicate :: `archiver_info_source_id` — and restore clears `archived_at` without re-activating,
duplicate :: because the same-generation snapshot cannot repair local drift. Deleting a reconciled item 409s (the next announcement would
duplicate :: recreate it); the throttle floor is released by an explicit operator cadence write, never
duplicate :: by reconciliation. Schedule resolution is four tiers under a floor:
duplicate :: announced → item → domain → system, then `max(resolved, throttle_floor)`.

# CR round 3, finding 14: the moved Archiver paragraph was the third statement
# of one fact in docs/ARCHITECTURE.md "## Archiver checkout location". Checked
# clause by clause against the bullets that remain there: ARCHIVER_REPO_PATH
# redirecting everything that needs the sibling repo (conftest's alembic run),
# the archiver-client path dependency on ../archiver/clients/python that no env
# var could redirect, and both halves now being gone — which is what "no second
# half to forget" says. Judged content-equivalence, as above.
duplicate :: **The Archiver checkout moves freely again (#254).** `ARCHIVER_REPO_PATH` now redirects
duplicate :: everything that needs the sibling repo — conftest's alembic run — because the
duplicate :: `archiver-client` path dependency that pinned `../archiver/clients/python` and honored no
duplicate :: env var went with the SDK. The old trap (setting one without the other, yielding passing
duplicate :: tests over a broken `uv sync`) no longer has a second half to forget.

# CR round 3, finding 15: this pointer named lifecycle/delete guards and the
# dashboard surfaces as living in docs/WATCHED-ITEMS.md; the split in this same
# branch moved both to docs/WATCHED-ITEMS-DASHBOARD.md. Rewritten into two
# pointers, one per doc, each naming what it actually contains.
retarget :: Fields, 3-tier schedule resolution, media-type dispatch, lifecycle and delete guards,
retarget :: template CRUD, and every dashboard surface: [docs/WATCHED-ITEMS.md](docs/WATCHED-ITEMS.md).
