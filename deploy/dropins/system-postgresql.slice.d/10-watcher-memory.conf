# Pass postgres's reservation through the slice Debian's templated unit sits in
# (watcher#309). Installed to /etc/systemd/system/system-postgresql.slice.d/.
# Without it, every postgresql@ instance's MemoryLow= is bounded to 0 here.
[Slice]
MemoryLow=384M
