# Information Service Phase 3a — Authoring Tools Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the authoring-tool surface to the Information service so Claude (or a human operator) can compose and validate Information Items + InfoSpecs end-to-end without touching the database or Watcher: `validate_info_spec`, `find_info_item`, atomic `create_info_item` (with optional initial InfoSpec), `fetch_and_render`, `preview_extraction`, `propose_selectors`. Each tool is a FastAPI route on the Information service (port 8020) and a corresponding ergonomic wrapper on the `information_client` SDK.

**Architecture:** Five new public endpoints under `/api/v1/tools/*` (read-only / non-mutating, except the atomic create extension on the existing `/info-items` route). The fetcher and HTML extractor are imported directly from Watcher's `src/core/` packages — same repo, same Python project, no duplication. Screenshot capture stays out of v1 (Playwright dependency is heavy and the design's `fetch_and_render` MVP only needs raw HTML + headers; screenshot lives in Phase 3a.1 once #3 lands the Playwright fetcher). All tool routes share the same `X-API-Key` auth dependency as the existing CRUD surface.

**Tech Stack:** FastAPI, Pydantic v2, SQLAlchemy 2 async, Postgres (Information schema), `jsonschema` (Draft 2020-12), `BeautifulSoup4` (selector proposal heuristics), `httpx.AsyncClient` (via Watcher's existing `HttpFetcher`), `information_client` SDK (extended), `openapi-python-client` (regeneration).

**Design doc:** `docs/plans/2026-05-03-information-source-specifications-design.md` § 6 ("Tools-first authoring lives on the Information service")
**Phase:** 3a (Information service tools); 3b (Archive service stand-up) is a separate plan.
**Issue:** #145

---

## File Structure

### New files

- `src/information/api/routes/tools.py` — FastAPI router mounting all five tool endpoints under `/api/v1/tools`. Each handler is a thin orchestration layer over a callable in `src/information/core/tools/`.
- `src/information/core/tools/__init__.py` — re-exports the tool implementations for testability.
- `src/information/core/tools/fetch_and_render.py` — `fetch_and_render(url, *, render: bool = False) -> FetchAndRenderResult`. Wraps `HttpFetcher` (from `src.core.fetchers.http`); `render=True` raises `NotImplementedError` until #3 lands.
- `src/information/core/tools/preview_extraction.py` — `preview_extraction(url, doc) -> PreviewExtractionResult`. Validates the InfoSpec document via the existing `validate_info_spec()`, fetches via `fetch_and_render`, extracts via `HtmlExtractor` (from `src.core.extractors`) with the same `_extraction_config_from_spec` translation Watcher uses (lift from `src/workers/pipeline.py:118-130` to a shared helper), computes the fingerprint per the spec's `fingerprint.algorithm`, and returns chunks + total bytes + computed fingerprint.
- `src/information/core/tools/propose_selectors.py` — `propose_selectors(url, description, *, top_k: int = 5) -> list[SelectorCandidate]`. v1 heuristic: `BeautifulSoup` over the fetched HTML; rank elements whose text contains the description (case-insensitive substring) by (a) selector specificity, (b) text length, (c) absence of obviously volatile classes (`__hash__`, `random`, hex/digit-only suffixes ≥ 8 chars). Returns selector + sample text + `stability_score: float`.
- `src/information/core/tools/find_info_item.py` — `find_info_item(session, query, limit) -> list[InfoItem]`. Trigram or `ILIKE '%q%'` over `name + description`; v1 uses `ILIKE` (no extension dependency); upgrade to `pg_trgm` if false-positive rates climb.
- `src/information/core/tools/extraction_config.py` — `extraction_config_from_spec(document) -> dict` lifted out of `src/workers/pipeline.py`. Watcher imports from here too (one of the modified files below).
- `src/information/api/schemas/tools.py` — Pydantic v2 request/response schemas: `FetchAndRenderRequest/Result`, `PreviewExtractionRequest/Result`, `ProposeSelectorsRequest/Result`, `SelectorCandidate`, `ValidateInfoSpecRequest/Result`, `FindInfoItemRequest`.
- `tests/information/api/test_tools_validate.py`, `test_tools_find.py`, `test_tools_fetch_and_render.py`, `test_tools_preview_extraction.py`, `test_tools_propose_selectors.py`, `test_create_info_item_atomic.py` — one test module per tool, mirroring `src/information/api/routes/`.
- `tests/information/core/tools/test_fetch_and_render.py`, `test_preview_extraction.py`, `test_propose_selectors.py`, `test_find_info_item.py`, `test_extraction_config.py` — unit tests for the core helpers.
- `clients/python/src/information_client/tools.py` — SDK section for tool methods (kept separate from `client.py` so the autogenerated section + the hand-written ergonomic wrappers don't collide). Mixed in via `class InformationClient` already in `client.py`.
- `tests/information_client/test_tools.py` — SDK-side tests over the new wrappers.
- `scripts/smoke_phase3a.sh` — end-to-end smoke: starts dev info server, calls each tool via curl + the SDK reference, asserts non-zero output. Mirrors `scripts/smoke_phase2c.sh`.

### Modified files

- `src/information/api/main.py` — register `tools.router` after the existing `info_items.router` and `info_specs.router`.
- `src/information/api/routes/info_items.py` — extend `POST /info-items` to accept an optional `initial_info_spec` field on `InfoItemCreate`. When provided: validate via `validate_info_spec()`, then in a single transaction insert the InfoItem row + the InfoSpec row at `priority=1, active=True`. On validation failure, return 422 with the validator's path/error fields. The atomic write is a single `session.commit()` — either both rows land or neither (default SQLAlchemy transaction semantics).
- `src/information/api/schemas/info_item.py` — add `initial_info_spec: dict | None = None` to `InfoItemCreate`. Add a top-level `InfoItemWithSpecOut` response variant returned only when `initial_info_spec` was supplied; otherwise the existing `InfoItemOut` shape stays unchanged.
- `src/information/core/info_spec_schema/validator.py` — extract a `validate_info_spec_with_errors(document) -> list[ValidationError]` variant that returns the structured error list instead of raising. The existing raise-based `validate_info_spec` stays for callers that just want the boolean answer. Tools layer uses the structured variant so it can return field-level errors in 422 responses.
- `src/workers/pipeline.py` — `_extraction_config_from_spec` is reduced to `from src.information.core.tools.extraction_config import extraction_config_from_spec` (re-export at module level for backward compat with tests; the helper now lives in the Information service's package since both consumers need it). No behaviour change.
- `clients/python/src/information_client/client.py` — `class InformationClient` gains the tool methods: `validate_info_spec(doc) -> ValidationResult`, `find_info_item(query, limit=20) -> list[InfoItemOut]`, `fetch_and_render(url, *, render=False) -> FetchAndRenderResult`, `preview_extraction(url, doc) -> PreviewExtractionResult`, `propose_selectors(url, description, top_k=5) -> list[SelectorCandidate]`, `create_info_item(name, description=None, owner=None, *, initial_info_spec=None) -> InfoItemOut | InfoItemWithSpecOut`. Wrappers call the autogenerated client functions (regenerated in Task 8).
- `clients/python/regen.sh` — already runs `openapi-python-client generate`; verify it picks up the new routes once they land. No edit expected.
- `docs/DEPLOYMENT.md` — add a "Tools surface" subsection under Information Service noting the `/api/v1/tools/*` prefix and the smoke script.
- `AGENTS.md` — note the tool surface in the "Watches & change bus" section's neighbour ("Information service authoring") with a one-line table of endpoints + their SDK methods.
- `README.md` — Architecture section gains one line: "Authoring tools live on the Information service at `/api/v1/tools/*` (Phase 3a)."

### Deleted files

- None expected.

### Out of scope (explicit)

- **Screenshot in `fetch_and_render`.** The MVP returns `screenshot_url: None`. Wire screenshot once #3 (Playwright fetcher) lands; until then `render=True` raises `NotImplementedError`.
- **Selector stability via render-then-replay.** v1 stability score is purely heuristic on the static HTML. A real stability score requires fetching the same page twice and diffing — deferred to a later iteration; not blocking Phase 3a.
- **`pg_trgm` extension for `find_info_item`.** v1 uses `ILIKE`. Upgrade if dataset grows past ~10k items or operators report false positives.
- **`spec.healed_via_fallback` / `spec.rebind_requested` events** (design § 7) — Phase 4 work.
- **Authoring tools on Watcher.** Watcher's tool surface (`create_watch`, `test_watch`) is design § 6 but stays out of Phase 3a — it's a Watcher concern, not an Information service one.
- **Archive service stand-up (port 8030).** Phase 3b — separate plan.
- **Multi-selector InfoSpecs (#141), fetch_config knobs (#142), inline InfoItem creation from the Watch form (#140).** Independent follow-ups; their plans land separately and may flow before or after Phase 3a depending on operator priorities.

---

## Cross-Cutting Decisions

**Tool routes mount under `/api/v1/tools/*`.** Keeps the read/write CRUD surface (`/api/v1/info-items`, `/api/v1/info-items/{id}/info-specs`) untouched and lets clients tell at a glance whether they're calling a registry mutation or a non-mutating helper. Auth is the same `X-API-Key` dependency used by the existing routes.

**Shared fetcher + extractor across services.** Watcher and the Information service live in the same uv project (`pyproject.toml` lists both as packages) and share the same Python interpreter at runtime. Importing `from src.core.fetchers.http import HttpFetcher` and `from src.core.extractors import HtmlExtractor` into `src/information/core/tools/` is a clean dependency direction (Information service depends on `src/core/`; Watcher's API and worker code depend on `src/core/` plus `src/api/` plus `src/workers/`; no cycle). When Information service eventually relocates off-VM (design § "Later"), the relevant `src/core/*` modules go with it as a shared lib package — out of scope for 3a but worth recording the direction.

**Overlap surface (today + post-3a).** Cataloguing the actual cross-service surface so future-us knows exactly what to lift if/when we extract a shared package:

| Module | Today | Post-3a | Notes |
|---|---|---|---|
| `src/core/logging.py` | shared (Information has thin adapter at `src/information/core/logging.py`) | unchanged | Adapter docstring already says "When the Information service extracts to its own repo, this module becomes its own logging configuration." |
| `src/core/fetchers/http.py` | Watcher-only | imported by `src/information/core/tools/fetch_and_render.py`, `propose_selectors.py`, `preview_extraction.py` | Pure HTTP client wrapper; no DB, no ORM, no Watcher-specific assumptions. |
| `src/core/extractors/` (`HtmlExtractor`, base `Extractor`, `Chunk`, `ExtractionResult`) | Watcher-only | imported by `src/information/core/tools/preview_extraction.py` | Pure transform: bytes → chunks. PDF/CSV extractors stay Watcher-only until Phase 3+ schema extension. |
| `src/core/simhash.py` | Watcher-only | imported by `src/information/core/tools/preview_extraction.py` for fingerprint computation | Single function, stdlib + a constant table. |
| `src/core/screenshot.py` | Watcher-only | unchanged in 3a (screenshot deferred to post-#3) | Will be shared in 3a.1 once Playwright lands. |

Modules that stay Watcher-specific and **must not** be imported from `src/information/`: `src/core/info_resolver.py` (consumes the SDK that Information owns — a cycle), `src/core/diff/`, `src/core/notifications/`, `src/core/probe.py`, `src/core/registry.py`, `src/core/storage/`, `src/core/scheduler/`, `src/core/rate_limiter.py`, `src/core/watches.py`, `src/core/crypto.py`, all of `src/core/models/` (Watcher's ORM tree).

The shared concerns are coherent — "URL → bytes → chunks → fingerprint" is the content-pipeline primitive. **Recommendation: do not extract a separate package in 3a.** The marginal value is low while the same uv project + same VM hosts both services; the right trigger for extraction is one of (a) Information service relocates off-VM, (b) Archive (3b) becomes the third importer of the same primitives, (c) we add a third consumer outside this repo. Tracked as a follow-up rather than a 3a deliverable; see "Open follow-ups" below.

**Claude tool surface — packaging via MCP.** The REST API + Python SDK are the source of truth, but Claude (in Claude Code, Claude Desktop, or via the API directly) discovers tools via MCP. Phase 3a ships an MCP server (`tools/information_mcp_server.py`) that imports the SDK and exposes each authoring tool as an MCP-discoverable tool with structured input schemas, "when to use" descriptions, and read-only / mutating annotations. This is the canonical Claude integration path going forward — shipping the API without an MCP wrapper would force every consumer to re-derive Claude tool descriptors locally. See Task 11 below.

**Atomic `create_info_item + initial_info_spec`.** A single SQLAlchemy session, one `commit()`. If validation fails, the InfoItem isn't created (pre-validation guard before any session.add). If the InfoSpec insert fails after the InfoItem add, the session rollback cleans both. The InfoSpec is always written at `priority=1, active=True` since it's the first one. The endpoint returns `InfoItemWithSpecOut` (carries both `info_item_id` and the new `info_spec_id`) so the caller can immediately reference the spec without a second round-trip.

**`validate_info_spec_with_errors` returns a list, not raises.** Routes need structured 422 bodies. The existing raise-based variant stays for callers that don't need the error list; we don't remove it.

**`propose_selectors` heuristic.** v1 is intentionally crude: substring match + specificity rank + volatility penalty. It's a tool to help an LLM agent narrow a search space, not a finished extraction recommendation. Operators always preview via `preview_extraction` before committing. If selector quality matters more than dev velocity, swap in a learned ranker — out of scope.

**`fetch_and_render` returns truncated bodies.** Body bytes capped at 5 MiB in the response. Larger pages are fetched in full server-side (extractor needs the whole DOM for `preview_extraction`) but the response truncates to keep client memory bounded. `truncated: bool` flag in the response.

**Test data isolation.** Tools that hit the network in tests are stubbed via the existing `respx`-style HTTP mocking already used in `tests/core/fetchers/test_http.py`. No live URLs in the suite.

**Tool-side rate limiting.** Out of scope for v1. The `X-API-Key` auth gate is enough — Watcher and Claude are the only callers, and neither will call `fetch_and_render` in tight loops.

---

## Task Breakdown

### Task 0: Pre-flight — file the GH issue, confirm Phase 2c is shipped, env vars present

- [ ] File a parent GH issue ("Phase 3a: Information service authoring tools"). Capture the goal + the design § 6 quote + a link to this plan.
- [ ] Verify `information.service` is active and reachable: `curl -s -H "X-API-Key: $INFORMATION_API_KEY" http://localhost:8020/api/v1/info-items` returns `[]` or a list.
- [ ] Verify `INFORMATION_BASE_URL` and `INFORMATION_API_KEY` are loaded in this shell session (`echo $INFORMATION_API_KEY` non-empty after the env export).
- [ ] Run the existing test suite + ruff on a clean `main`: 711 unit + 611 integration must pass before any change.
- [ ] Branch: `feat/<issue>-information-phase3a-tools`.

### Task 1: Lift `extraction_config_from_spec` into the shared package + write its first test

- [ ] Red: write `tests/information/core/tools/test_extraction_config.py` covering the same three cases as `tests/workers/test_pipeline.py::TestExtractionConfigFromSpec`. Tests import from `src.information.core.tools.extraction_config`. Run — fails because module doesn't exist.
- [ ] Green: create `src/information/core/tools/__init__.py` and `extraction_config.py`. Move the function body from `src/workers/pipeline.py` verbatim. Run — passes.
- [ ] Refactor: in `src/workers/pipeline.py`, replace the inline `_extraction_config_from_spec` with `from src.information.core.tools.extraction_config import extraction_config_from_spec as _extraction_config_from_spec` (alias preserves the underscore-prefixed contract for any test that imports it). Run worker tests — still pass.

### Task 2: `validate_info_spec` route + SDK wrapper

- [ ] Red: `tests/information/api/test_tools_validate.py` — POST `/api/v1/tools/validate-info-spec` with a valid spec returns `{"valid": true, "errors": []}`; with an invalid spec (e.g. missing `fingerprint`) returns 200 with `{"valid": false, "errors": [{...path, message...}]}`. (200 not 422 — validation is the *purpose* of the endpoint.)
- [ ] Green: add `validate_info_spec_with_errors(document) -> list[ValidationError]` to `src/information/core/info_spec_schema/validator.py`. Add `tools.router` and the `validate_info_spec` route. Mount under `/api/v1/tools` in `src/information/api/main.py`.
- [ ] SDK: add `InformationClient.validate_info_spec(doc) -> ValidationResult` wrapper. Test in `tests/information_client/test_tools.py`.
- [ ] Smoke: `curl` the endpoint locally with a valid + invalid doc.

### Task 3: `find_info_item` route + SDK wrapper

- [ ] Red: `tests/information/api/test_tools_find.py` — seed three InfoItems with distinct names; GET `/api/v1/tools/find-info-items?q=foo&limit=10` returns only the matching ones. Empty `q` returns 422 (required).
- [ ] Green: `find_info_item(session, query, limit)` in `src/information/core/tools/find_info_item.py`. Route in `tools.py`. `ILIKE '%query%'` over `name + description`; order by `created_at desc`.
- [ ] SDK: `InformationClient.find_info_item(query, limit=20) -> list[InfoItemOut]`.
- [ ] Smoke: live curl + SDK call.

### Task 4: Atomic `create_info_item` extension — `initial_info_spec`

- [ ] Red: `tests/information/api/test_create_info_item_atomic.py`. Three cases: (a) POST without `initial_info_spec` returns the existing `InfoItemOut` shape (regression guard); (b) POST with a valid `initial_info_spec` returns `InfoItemWithSpecOut` carrying both IDs and exactly one row in `info_specs` at priority 1, active true; (c) POST with an invalid `initial_info_spec` returns 422 and **no rows** in either `info_items` or `info_specs` for the attempted name.
- [ ] Green: add `initial_info_spec` to `InfoItemCreate`. Add `InfoItemWithSpecOut` response model. Update the route handler — guard validation at the top, then create both rows in one transaction.
- [ ] SDK: extend `InformationClient.create_info_item` signature with `initial_info_spec=None`. Return type union: `InfoItemOut | InfoItemWithSpecOut`.
- [ ] Smoke: round-trip an InfoItem + spec via the SDK, then call `get_primary_info_spec` on the new item and assert it matches.

### Task 5: `fetch_and_render` route + SDK wrapper (no Playwright)

- [ ] Red: `tests/information/api/test_tools_fetch_and_render.py` — POST `/api/v1/tools/fetch-and-render` with a URL backed by `respx` returns `{url, status_code, headers, body, body_bytes_total, truncated, screenshot_url: null}`. `render=true` returns 501 with `Playwright fetcher not yet integrated (#3)`.
- [ ] Green: implement `fetch_and_render(url, render=False)` in `src/information/core/tools/`. Use `HttpFetcher` (instantiate inline; this endpoint doesn't need the singleton ServiceRegistry pattern). Cap response body at 5 MiB; set `truncated=True` if over.
- [ ] SDK wrapper.
- [ ] Smoke.

### Task 6: `preview_extraction` route + SDK wrapper

- [ ] Red: `tests/information/api/test_tools_preview_extraction.py`. Three cases: (a) valid spec + URL with content returns `{chunks: [...], total_chars, computed_fingerprint, fingerprint_algorithm}`; (b) invalid spec returns 422 with the validator errors; (c) URL fetch failure returns 422 with `target_unreachable`.
- [ ] Green: implement `preview_extraction(url, doc)`. Validate via `validate_info_spec_with_errors` first; on errors, return them as 422. Fetch via the new `fetch_and_render` helper. Extract via `HtmlExtractor` configured by `extraction_config_from_spec(doc)`. Compute fingerprint via the helper Watcher uses (verify which module it lives in — likely `src/core/simhash.py` for simhash; for sha256 it's stdlib).
- [ ] SDK wrapper.
- [ ] Smoke.

### Task 7: `propose_selectors` route + SDK wrapper

- [ ] Red: `tests/information/api/test_tools_propose_selectors.py`. Cases: (a) HTML containing the description text in a stable `<h1>` ranks the `<h1>` selector above noisy ones; (b) volatile classes (`hash-abc123`) penalised; (c) zero matches returns `[]`, not an error.
- [ ] Green: implement `propose_selectors(url, description, top_k)`. Fetch via `fetch_and_render`. Parse with `BeautifulSoup4` (already a dep — verify in `pyproject.toml`; add if not). Walk all elements; for each whose `.get_text()` (case-insensitive) contains the description, compute selector + stability score (specificity bonus, length bonus, volatility penalty). Sort, return top_k.
- [ ] SDK wrapper.
- [ ] Smoke.

### Task 8: SDK regen + ergonomic-wrapper polish

- [ ] Run `clients/python/regen.sh` to regenerate the autogenerated client section against the live OpenAPI spec.
- [ ] Verify the regenerated `information_client._gen` package picks up all five new routes (`validate_info_spec`, `find_info_item`, `fetch_and_render`, `preview_extraction`, `propose_selectors`) and the extended `create_info_item`.
- [ ] Reconcile the hand-written wrappers with the regenerated function names; update imports.
- [ ] Bump SDK version in `clients/python/pyproject.toml`.
- [ ] Re-run all SDK tests.

### Task 9: End-to-end smoke + reference flow

- [ ] Author `scripts/smoke_phase3a.sh`. Mirrors `scripts/smoke_phase2c.sh` style: starts the dev info server, exercises each tool via the SDK, asserts on output. Bails on any non-zero curl/SDK call.
- [ ] Reference flow exercised in the smoke: `find_info_item` (empty result) → `propose_selectors` against a fixture URL → `preview_extraction` with the top selector → `validate_info_spec` of the assembled doc → `create_info_item` with `initial_info_spec` → `get_primary_info_spec` round-trip → confirms the full authoring loop.

### Task 10: Documentation sweep

- [ ] `AGENTS.md` — short table of tool endpoints + SDK methods in the "Information service" subsection.
- [ ] `README.md` — one-line addition to Architecture pointing at `/api/v1/tools/*`.
- [ ] `docs/DEPLOYMENT.md` — "Tools surface" subsection under Information Service.
- [ ] `clients/python/README.md` (if exists) — extended SDK examples.

### Task 11: MCP server for Claude tool discovery (deferred to #147)

> **Status:** Deferred to a focused follow-up PR — tracked at [#147](https://github.com/CannObserv/watcher/issues/147). Tasks 1-10 + 12 ship as the first Phase 3a slice; the MCP wrapper is a discrete feature with its own dependency surface (the `mcp` Python SDK), descriptor-shape testing, and Claude Code wiring docs. Splitting it off keeps each PR a single coherent change. The plan steps below are kept as the spec for the follow-up.

**Why it's part of 3a, not deferred.** Claude (in Claude Code and Claude Desktop) discovers tools via the Model Context Protocol. Without an MCP server, every Claude consumer has to hand-derive tool descriptors from the OpenAPI spec — losing the "when to use" hints, safety annotations, and concrete examples that make tool selection reliable. Shipping the MCP wrapper alongside the API is the difference between a tool surface that's *usable by Claude* and one that's *theoretically callable*. Best practices encoded here are drawn from MCP server conventions and Anthropic's tool-use guidance.

- [ ] **Scaffold**: `tools/information_mcp_server.py`. Uses the `mcp` Python SDK (add to `[project.optional-dependencies] mcp = ["mcp>=…"]` in `pyproject.toml`). Stdio transport (the default for `claude mcp add`).
- [ ] **One MCP tool per SDK method.** Each tool wraps a `InformationClient` call. Tools to expose: `validate_info_spec`, `find_info_item`, `fetch_and_render`, `preview_extraction`, `propose_selectors`, `create_info_item` (with optional `initial_info_spec`), `create_info_spec`, `get_info_item`, `list_info_items`, `get_primary_info_spec`, `list_active_info_specs`, `patch_info_spec`. Read-only CRUD (get/list) is included so Claude can introspect existing items without round-tripping through the API directly.
- [ ] **Descriptions answer "when to use", not "what it does".** Each MCP tool descriptor's `description` field explains: the *intent* the tool serves, *when* in an authoring flow to call it, and *what comes next*. Example for `propose_selectors`: "Use after `fetch_and_render` returns the page HTML and you've described the target content in plain language. Returns ranked CSS selector candidates with stability scores. Always verify the chosen candidate via `preview_extraction` before persisting via `create_info_spec`."
- [ ] **Concrete examples** in each tool's description (`Args.example` per the MCP SDK). One canonical input/output pair per tool. Examples are linked to fixture URLs from the smoke test so they stay current.
- [ ] **Safety annotations.** Tag each tool's metadata with `read_only: bool` and (where applicable) `requires_confirmation: bool`. Read-only set: `validate_info_spec`, `find_info_item`, `fetch_and_render`, `preview_extraction`, `propose_selectors`, `get_info_item`, `list_info_items`, `get_primary_info_spec`, `list_active_info_specs`. Mutating set: `create_info_item`, `create_info_spec`, `patch_info_spec`. The MCP host (Claude Code) uses these to decide whether to prompt the user before invocation.
- [ ] **Structured errors.** Each tool returns either a successful result or `{"error": "<code>", "details": {...}}` — never free-text 500s. Error codes: `validation_failed`, `target_unreachable`, `not_found`, `auth_error`, `server_error`. This lets Claude self-recover (e.g. on `validation_failed`, fetch the validator's error list and retry with a corrected doc).
- [ ] **Configuration.** MCP server reads `INFORMATION_BASE_URL` and `INFORMATION_API_KEY` from env. No additional secrets. Document the `claude mcp add information --command "uv run python tools/information_mcp_server.py"` invocation in `docs/DEPLOYMENT.md`.
- [ ] **Tests.** `tests/tools/test_information_mcp_server.py` — start the server in-process, list tools via MCP introspection, invoke each tool against a stubbed SDK, assert the descriptors carry descriptions + examples + safety annotations.
- [ ] **Smoke.** Add an MCP-side check to `scripts/smoke_phase3a.sh`: connect, list tools, call `validate_info_spec` end-to-end. Bails on any missing tool or wrong descriptor shape.

### Task 12: Final verification

- [ ] Full pytest suite green (unit + integration). Coverage non-regressive.
- [ ] Ruff lint + format clean.
- [ ] `scripts/smoke_phase3a.sh` passes against the dev info server (port 8021) and against the systemd service (port 8020).
- [ ] Restart `information.service`; tail logs; confirm no crash on lifespan.
- [ ] Watcher service stays green (the only Watcher-side change is the import shim in Task 1).
- [ ] PR drafted; CR pass; merge to `main`; close the parent issue.

---

## Wrap-up

After Task 12:
- Information service exposes five new tool endpoints under `/api/v1/tools/*`, all backed by structured Pydantic schemas and the existing `X-API-Key` auth.
- `POST /api/v1/info-items` accepts an optional `initial_info_spec` for atomic two-row creation.
- The `information_client` SDK exposes ergonomic wrappers for every tool; the autogenerated client section is regenerated against the live spec.
- Watcher's `_extraction_config_from_spec` is now a re-export from `src/information/core/tools/extraction_config.py`; both services use the same translation logic.
- An MCP server (`tools/information_mcp_server.py`) exposes every tool to Claude with "when to use" descriptions, concrete examples, and read-only / mutating safety annotations. `claude mcp add information …` lights it up locally.
- A reference smoke script proves the full author-an-InfoItem-then-monitor-it loop end-to-end, including an MCP introspection round-trip.

**Open follow-ups (deferred):**
- Screenshot in `fetch_and_render` — wire once #3 (Playwright) lands.
- Selector stability via render-then-replay (currently heuristic only).
- Replace the heuristic `propose_selectors` ranker with a learned model — research issue #146.
- `pg_trgm` for `find_info_item` once dataset size or false-positive rate justifies it.
- Tool surface on Watcher (`create_watch`, `test_watch`) — the symmetric piece on the monitoring side; lives in a Watcher-side plan if/when scheduled.
- Phase 3b — Archive service stand-up (port 8030, consumes `info.changes`, archives origin payloads).
- **Shared "content-pipeline" lib extraction.** Lift `src/core/fetchers/`, `src/core/extractors/`, `src/core/simhash.py`, `src/core/screenshot.py`, `src/core/logging.py` into a standalone Python package consumed by Watcher, Information service, and (future) Archive. Triggers: Information relocates off-VM, Archive (3b) becomes the third importer, or a fourth consumer materialises outside this repo. Until any of those, the in-repo same-uv-project layout pulls its weight without packaging overhead.

**Risk register:**
- **Cross-package import direction.** `src/information/` imports from `src/core/`. As long as the reverse direction is enforced via lint or convention, no cycle. Off-VM extraction later will need a shared lib refactor; out of scope.
- **`fetch_and_render` blocks.** A slow target ties up an event loop slot on the Information service. With the FastAPI default (uvicorn auto worker count) this is acceptable; revisit if operators report tool-call latency under contention.
- **Heuristic selector quality.** `propose_selectors` will sometimes recommend bad selectors. Operators always validate via `preview_extraction`; the workflow assumes review, not blind trust.
- **Body cap at 5 MiB.** Some target pages exceed this. The cap only affects the *response* body — server-side extraction still sees the full page. If operators need the full body in their response, raise the cap or stream.
- **SDK regen drift.** `regen.sh` overwrites the autogenerated section. The hand-written wrappers in `clients/python/src/information_client/tools.py` (or the `client.py` extensions) must stay in a clearly separated section so regen doesn't clobber them. Verify after the first regen.

---

## References

- Design doc: `docs/plans/2026-05-03-information-source-specifications-design.md` § 6 (Tools-first authoring)
- Phase 1 plan: `docs/plans/2026-05-03-information-service-phase1-plan.md`
- Phase 2c plan: `docs/plans/2026-05-04-watcher-phase2c-cutover-plan.md`
- Predecessor issue: #138 (closed)
- SDK regeneration: `clients/python/regen.sh`
- Smoke pattern: `scripts/smoke_phase2c.sh`
